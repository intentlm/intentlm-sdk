# SDK build configuration (optional)

# Override the default inference API base (includes /v1)
# INTENTLM_API_URL=https://api.example.com/v1

# Set to 'true' for unminified debug builds
DEBUG_BUILD=false
